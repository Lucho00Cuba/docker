<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<properties>
   <comment>PTM Configuration</comment>

<!--
    PTM configurable properties

    Configurable properties can be set by PTM users based on their specific
    environment. Some configurable properties have default values, as described 
    below.

    - ThreadPoolSize: 
        The ThreadPoolSize is the maximum number of threads used for
        running policies.  The default is 10.

    - MailSmtpHost:
        The SMTP host name. If it's specified but not valid, PTM will simply
        ignore. If it's not specified, and email notification address is 
        specified in policy, then an error will be logged. 
        Sample value: smtp.sampledomain.com
    
    - MailSmtpPort:
        The SMTP port number. The default is 25. 

    - MailSmtpFrom:
        The sender's email address. If it's not specified, the "From" field
        of the email notification will be set to blank.
    	Sample value: admin@companyname.com
    	
    - MailSmtpUser:
        The user's account, used for SMTP authentication. This property must be
        specified if and only if the SMTP server requires authentication. If a 
        wrong user's acccount/password pair is specified, an authentication 
        error will be logged. 
        Please do NOT specify blank value for this property. 
        Sample value: admin
        
    - MailSmtpPassword:
        The user's password, used for SMTP authentication. This property must be
        specified if and only if the MailSmtpUser property is specified.
        
    If you would like to specify or customize the PTM configurable properties, 
    please uncomment out the corresponding entries below and provide desired 
    values.
-->

  <!--entry key="ThreadPoolSize">10</entry-->
  <!--entry key="MailSmtpHost"></entry-->
  <!--entry key="MailSmtpPort"></entry-->
  <!--entry key="MailSmtpFrom"></entry-->
  <!--entry key="MailSmtpUser"></entry>
  <entry key="MailSmtpPassword"></entry-->
  <entry key="EnableMetricCache">FALSE</entry>
</properties>

